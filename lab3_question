1.Create a class author with atributes name and qualification also create a class publication with atributes pname from these classes derive a class book having attribute title and price.Each of three classes should have a get_data() method to get there datafrom the user and these classes should have put_data() method to display the data. create instance of the classes book in the main().
